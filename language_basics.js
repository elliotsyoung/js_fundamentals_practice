// There are a few unusual langauge features in JS:

// 1. Variables are hoisted to the top wherever they are scoped. when you use Variables not constant



console.log(name);
var name = "elliot";

let obj = {
  name: "justin",
  age: 29
}

console.log(obj.last_name);
